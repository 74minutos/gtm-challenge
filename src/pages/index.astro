---
import Layout from '../layouts/Layout.astro'
import { bloques } from '../content/course'
---

<Layout title="La Evolución del Tagging | Curso GTM">
  <!-- Hero -->
  <section class="min-h-[90vh] flex items-center justify-center px-6 py-20">
    <div class="max-w-4xl mx-auto text-center">
      <span class="pill mb-6">Curso interactivo</span>
      
      <h1 class="text-5xl md:text-7xl font-bold tracking-tight leading-tight mb-6">
        La Evolución del
        <span class="text-primary-dark">Tagging</span>
      </h1>
      
      <p class="text-xl md:text-2xl text-ink-light mb-4 max-w-2xl mx-auto">
        De cero a estratega de datos.
      </p>
      
      <p class="text-lg text-ink-light/70 mb-12 max-w-xl mx-auto">
        Un viaje gamificado para dominar Google Tag Manager, 
        entender los datos y tomar decisiones que importan.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/seccion-0" class="btn-primary text-lg px-8 py-4">
          Comenzar aventura →
        </a>
        <a href="#contenido" class="btn-secondary text-lg px-8 py-4">
          Ver contenido
        </a>
      </div>

      <div class="flex justify-center gap-8 mt-16 text-sm text-ink-light">
        <div class="text-center">
          <p class="text-2xl font-bold text-ink">8</p>
          <p>Bloques</p>
        </div>
        <div class="text-center">
          <p class="text-2xl font-bold text-ink">~4h</p>
          <p>Total</p>
        </div>
        <div class="text-center">
          <p class="text-2xl font-bold text-ink">15+</p>
          <p>Ejercicios</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Contenido -->
  <section id="contenido" class="px-6 py-20 bg-cream-dark">
    <div class="max-w-5xl mx-auto">
      <h2 class="text-3xl md:text-4xl font-bold text-center mb-4">
        Tu viaje de aprendizaje
      </h2>
      <p class="text-ink-light text-center mb-16 max-w-2xl mx-auto">
        Cada bloque te lleva un paso más cerca de dominar GTM. 
        Sin atajos, sin humo, con práctica real.
      </p>

      <div class="grid md:grid-cols-2 gap-6">
        {bloques.map((bloque) => (
          <a 
            href={`/${bloque.id}`}
            class="card group hover:border-primary transition-all duration-300"
          >
            <div class="flex items-start gap-4">
              <span class="text-4xl">{bloque.icono}</span>
              <div class="flex-1">
                <div class="flex items-center gap-2 mb-2">
                  <span class="text-xs font-medium text-ink-light uppercase tracking-wide">
                    {bloque.numero === 0 ? 'Intro' : `Bloque ${bloque.numero}`}
                  </span>
                  <span class="text-xs text-ink-light/60">•</span>
                  <span class="text-xs text-ink-light/60">{bloque.duracion}</span>
                </div>
                <h3 class="text-xl font-semibold mb-1 group-hover:text-primary-dark transition-colors">
                  {bloque.titulo}
                </h3>
                <p class="text-sm text-ink-light mb-3">{bloque.subtitulo}</p>
                <p class="text-sm text-ink-light/70">{bloque.descripcion}</p>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Final -->
  <section class="px-6 py-20">
    <div class="max-w-3xl mx-auto text-center">
      <h2 class="text-3xl font-bold mb-6">
        ¿Listo para empezar?
      </h2>
      <p class="text-ink-light mb-8">
        No necesitas conocimientos previos. Solo curiosidad y ganas de entender cómo funcionan los datos.
      </p>
      <a href="/seccion-0" class="btn-primary text-lg px-10 py-4">
        Comenzar el curso →
      </a>
    </div>
  </section>

  <!-- Footer -->
  <footer class="px-6 py-10 border-t border-cream-dark text-center text-ink-light text-sm">
    <p>La Evolución del Tagging © {new Date().getFullYear()}</p>
  </footer>
</Layout>
