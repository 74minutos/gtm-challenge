---
import BloqueLayout from '../layouts/BloqueLayout.astro'
import { bloques } from '../content/course'

const bloqueId = Astro.url.pathname.replace('/', '')
const bloque = bloques.find(b => b.id === bloqueId)
---

<BloqueLayout bloqueId={bloqueId} title={bloque?.titulo || 'Bloque'}>
  <div class="max-w-3xl mx-auto px-6">
    <section class="py-12 text-center">
      <span class="pill mb-4">{bloque?.numero === 0 ? 'Intro' : `Bloque ${bloque?.numero}`} · {bloque?.duracion}</span>
      <h1 class="text-4xl md:text-5xl font-bold mb-4">
        {bloque?.icono} {bloque?.titulo}
      </h1>
      <p class="text-xl text-ink-light">{bloque?.subtitulo}</p>
    </section>

    <section class="py-12">
      <div class="card text-center">
        <p class="text-6xl mb-4">🚧</p>
        <h2 class="text-2xl font-bold mb-4">Contenido en construcción</h2>
        <p class="text-ink-light mb-6">{bloque?.descripcion}</p>
        <p class="text-sm text-ink-light/60">
          Este bloque se migrará próximamente del proyecto original.
        </p>
      </div>
    </section>
  </div>
</BloqueLayout>
